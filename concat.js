const hege = [ "Cecilie", "Lone" ];
const stale = [ "Emil", "Tobias", "Linus" ];

// concat method joint  the once array to another array and return the new array and don't override the original array
const children = hege.concat( stale );
console.log( children );
console.log( hege );
console.log( stale );
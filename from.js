const names = "ABCDEFG";

// from method take the string and iterates all items and convert the array string
// just we can say from method is string iterable function
const iterate = Array.from( names );

console.log( iterate );
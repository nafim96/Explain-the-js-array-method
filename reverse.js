const fruits = [ "Banana", "Orange", "Apple", "Mango" ];
const revers = fruits.reverse();

//when use the revers function it's self override the original array
console.log( revers );

// now use the original array but it's changed
console.log( fruits );


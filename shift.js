const fruits = [ "Banana", "Orange", "Apple", "Mango" ];

// shift change the original array and remove the first element from array
const newFruits = fruits.shift();

// return the remove element from array
console.log( newFruits );

// original array is changed
console.log( fruits );